using UnityEngine;


public class KinematicHandler : MonoBehaviour
{
    /*
    -LINEAR MOTION:
    v = v0 + a*t
    dx = (v+v0)/2*t
    dx = v0*t + 1/2*a*t^2
    v^2 = v0^2 + 2*a*dx

    -ROTATIONAL MOTION:
    w = w0 + a*t
    theta = 1/2(w+w0)*t
    theta = w0*t+1/2*a*t^2
    w^2 = w0^2 +2*a*theta
    */

    
}